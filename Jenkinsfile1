pipeline {
    agent any

    tools {
        maven 'maven3.8.2'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Build') {
            steps {
                sh "mvn clean package -DskipTests"
            }
        }
    }
}
